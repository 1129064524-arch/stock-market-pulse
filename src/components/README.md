# Frontend Modules

- `Sidebar.jsx`: desktop navigation and module grouping.
- `market.jsx`: normalized fallback data, market formatting helpers, and shared chart components.
- `navigation.js`: sidebar information architecture.

Page modules live in `src/pages/`, one file per sidebar workspace. `App.jsx` owns cross-page state, API requests, and page composition.
